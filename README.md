Install Dotnet SDK 9 (min): `winget install Microsoft.DotNet.SDK.9`

Install Dotnet-Script tool: `dotnet tool install -g dotnet-script`

Add dps.report links to `reports.txt` (no blank lines)

Run Script Terminal: `dotnet-script .\main.csx` (Or double click `main.csx`, choosing `.NET Host` if prompted)

???

Profit with `summary-report.txt`
